project.name = shopnc
server.index = 127.0.0.1:8383
server.search = 127.0.0.1:8384

[pk]
type = id

[goods_id]
type = numeric

[goods_name]
type = title
index = mixed
weight = 5
tokenizer = split(-)

[gc_name]
index = mixed
weight = 3

[goods_jingle]
index = mixed

[brand_name]
index = mixed
weight = 4

[cate_1]
index = self
tokenizer = full

[cate_2]
index = self
tokenizer = full

[cate_3]
index = self
tokenizer = full

[brand_id]
index = self
tokenizer = full

[transport_id]
index = self
tokenizer = full

[brand_country_code]
index = self
type = string
tokenizer = full

[attr_id]
index = self
tokenizer = split(_)

[store_id]
index = self
tokenizer = full

[goods_promotion_price]
type = numeric

[goods_click]
type = numeric

[goods_salenum]
type = numeric

[goods_barcode]
index = self
tokenizer = full

[gc_id]
index = none

[main_body]
index = none
